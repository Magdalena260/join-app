import { Component } from '@angular/core';

/**
 * Component managing the structural binding and static rendering of the application's data protection declarations.
 * Encapsulates the explicit privacy policy view context to satisfy regulatory compliance and user transparency constraints across the architecture.
 */
@Component({
  selector: 'app-privacy-policy',
  imports: [],
  templateUrl: './privacy-policy.html',
  styleUrl: './privacy-policy.scss',
})
export class PrivacyPolicy {}
